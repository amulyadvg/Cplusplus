#include <iostream>
#include<math.h>
using namespace std;

#define SWAP(a,b) {int temp = a; a = b; b = temp;}

int main() {
  int num1, num2;
  cin >> num1 >> num2;
  int result;
  
  result = SWAP(num1, num2);
  cout << result << endl;
}
